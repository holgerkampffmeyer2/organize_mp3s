name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pyinstaller

      - name: Download ffmpeg (static)
        run: |
          curl -L -o ffmpeg.tar.xz https://johnvansickle.com/ffmpeg/releases/ffmpeg-release-amd64-static.tar.xz
          tar xf ffmpeg.tar.xz
          cp ffmpeg-*-static/ffmpeg pyinstaller/ffmpeg
          chmod +x pyinstaller/ffmpeg

      - name: Build binary
        run: pyinstaller pyinstaller/organize.spec

      - name: Create archive
        run: |
          cd dist/organize-mp3s
          tar czf ../organize-mp3s-linux-amd64.tar.gz *

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-amd64
          path: dist/organize-mp3s-linux-amd64.tar.gz

  build-macos:
    runs-on: macos-latest
    strategy:
      matrix:
        arch: [arm64, x86_64]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pyinstaller

      - name: Download ffmpeg (static)
        run: |
          if [ "${{ matrix.arch }}" = "arm64" ]; then
            curl -L -o ffmpeg.zip https://evermeet.cx/ffmpeg/getrelease/ffmpeg/zip
          else
            curl -L -o ffmpeg.zip https://evermeet.cx/ffmpeg/getrelease/ffmpeg/zip?arch=x86_64
          fi
          unzip ffmpeg.zip -d pyinstaller/
          chmod +x pyinstaller/ffmpeg

      - name: Build binary
        run: pyinstaller pyinstaller/organize.spec

      - name: Create archive
        run: |
          cd dist/organize-mp3s
          tar czf ../organize-mp3s-macos-${{ matrix.arch }}.tar.gz *

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-${{ matrix.arch }}
          path: dist/organize-mp3s-macos-${{ matrix.arch }}.tar.gz

  release:
    needs: [build-linux, build-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            linux-amd64/organize-mp3s-linux-amd64.tar.gz
            macos-arm64/organize-mp3s-macos-arm64.tar.gz
            macos-x86_64/organize-mp3s-macos-x86_64.tar.gz
          generate_release_notes: true
          draft: false
          prerelease: false
